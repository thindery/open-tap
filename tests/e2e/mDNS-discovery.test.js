import { describe, it } from 'vitest';

describe('mDNS Discovery (Manual/Network)', () => {
  it.skip('should discover peers on the same network', async () => {
    // This requires actual multicast network support
    // Usually skipped in CI environments
  });
});
